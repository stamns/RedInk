# ===========================================
# API 配置
# ===========================================

# Google Gemini API Key（用于文案生成）
# 获取地址: https://ai.google.dev/
GOOGLE_CLOUD_API_KEY=your_google_cloud_api_key_here

# 图片生成 API Key（用于生成图片）
# 配合 image_providers.yaml 中的配置使用
IMAGE_API_KEY=your_image_api_key_here

# ===========================================
# 图片服务商高级配置
# ===========================================

# 图片服务商配置 JSON/YAML 内容（可选）
# 如果设置，将覆盖 image_providers.yaml 中的配置
# 优先级：IMAGE_PROVIDERS_CONFIG_BASE64 > IMAGE_PROVIDERS_CONFIG > image_providers.yaml > 默认配置
# 示例：IMAGE_PROVIDERS_CONFIG={"active_provider": "google_genai", "providers": {...}}
IMAGE_PROVIDERS_CONFIG=

# 图片服务商配置 Base64 编码内容（可选）
# 适用于复杂的配置内容，避免特殊字符问题
# 内容为 JSON 或 YAML 字符串的 Base64 编码
IMAGE_PROVIDERS_CONFIG_BASE64=

# 文字生成 API Key（如果使用自定义文字生成服务需要配置）
TEXT_API_KEY=your_text_api_key_here

# 文字生成 API Base URL（可选）
# 如果使用自定义的文字生成服务，可以配置此项
# 默认: https://api.example.com
TEXT_API_BASE_URL=https://api.bltcy.ai

# ===========================================
# Flask 服务配置
# ===========================================

# 调试模式（开发环境建议 True，生产环境改为 False）
FLASK_DEBUG=True

# 服务监听地址（0.0.0.0 表示允许外部访问）
FLASK_HOST=0.0.0.0

# 服务端口
FLASK_PORT=12398

# ===========================================
# CORS 跨域配置
# ===========================================

# 允许的前端地址（多个地址用逗号分隔）
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ===========================================
# 文件存储配置
# ===========================================

# 生成图片的输出目录
OUTPUT_DIR=output

# ===========================================
# 存储配置
# ===========================================

# 存储后端: local (默认) 或 vercel_blob
STORAGE_BACKEND=local

# Vercel Blob 配置 (STORAGE_BACKEND=vercel_blob 时需要)
VERCEL_BLOB_READ_WRITE_TOKEN=your_vercel_blob_rw_token
VERCEL_BLOB_URL=https://your-store.public.blob.vercel-storage.com

# Vercel KV / Upstash Redis 配置 (STORAGE_BACKEND=vercel_blob 时需要)
VERCEL_KV_REST_API_URL=https://your-kv-url.upstash.io
VERCEL_KV_REST_API_TOKEN=your_kv_token

